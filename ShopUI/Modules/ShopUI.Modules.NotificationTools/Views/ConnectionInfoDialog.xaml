<UserControl x:Class="ShopUI.Modules.NotificationTools.Views.ConnectionInfoDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <UniformGrid Columns="2" Grid.Row="0">
            <TextBlock Text="ConnectionString" Margin="5,0,5,0"/>
            <TextBlock Text="{Binding ConnectionString,Mode=OneWay}" TextWrapping="Wrap"/>
            <TextBlock Text="ConnectionState" Margin="5,0,5,0"/>
            <TextBlock Text="{Binding ConnectionState,Mode=OneWay}"/>
        </UniformGrid>

        <Button Command="{Binding CloseCommand}" 
                Content="Ok" 
                FontSize="10"
                IsEnabled="True"
                Margin="10" Cursor="Hand"
                Grid.Row="1"/>
        
    </Grid>
</UserControl>
